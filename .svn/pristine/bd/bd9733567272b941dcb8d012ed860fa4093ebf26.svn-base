package com.akito.tools.javac.comp;

/**
 * @author THINK
 *在当前定义的类的CClassDef对象（即m_FileNode.pClassDefList链的链头CClassDef节点）的
 *pBaseClassList链的链头插入一个CBaseClass节点。
在新创建的CBaseClass节点里，填下面的域：
pClassDef = 当前定义的类的CClassDef对象的地址。
ucAccessPrivilege = 继承方式。若是public继承，则该域置1；反之，置0。

 */
public class JBaseClass {
	
	private JClassDef classDef;
	
	private int AccessPrivilege;
	
}
